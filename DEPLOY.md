# 🚀 Деплой на Render.com (безкоштовно)

## Швидкий деплой (5 хвилин)

### 1️⃣ Створити Git репозиторій

```bash
cd "/Users/pavellizov/Desktop/Main_1/КСЮША РАБОТОДАТЕЛЬ/скам/us.sitesucker.mac.sitesucker-pro/www.wask.co"

# Ініціалізувати git
git init

# Додати файли
git add .

# Зробити коміт
git commit -m "Instagram Comment Picker - готово до деплою"
```

### 2️⃣ Завантажити на GitHub

1. Створіть новий репозиторій на https://github.com/new
2. Назвіть його: `instagram-comment-picker`
3. НЕ додавайте README/LICENSE/.gitignore (вони вже є)
4. Виконайте команди:

```bash
git remote add origin https://github.com/YOUR_USERNAME/instagram-comment-picker.git
git branch -M main
git push -u origin main
```

### 3️⃣ Деплой на Render.com

1. **Зареєструйтесь:** https://render.com/ (безкоштовно)
2. **Натисніть:** "New +" → "Web Service"
3. **Підключіть GitHub:** дозвольте доступ до репозиторію
4. **Налаштування:**
   - Name: `instagram-picker`
   - Environment: `Node`
   - Build Command: `npm install`
   - Start Command: `node server.js`
   - Instance Type: **Free** (0$)
5. **Натисніть:** "Create Web Service"

⏳ **Очікування:** 5-10 хвилин

✅ **Готово!** Ваш API буде доступний на: `https://instagram-picker.onrender.com`

---

## 4️⃣ Оновити фронтенд

Відкрийте `picker-local.html` та змініть:

```javascript
// Було:
const API_URL = 'http://localhost:3000';
let useRealAPI = false;

// Стало:
const API_URL = 'https://instagram-picker.onrender.com'; // Ваша адреса з Render
let useRealAPI = true;
```

---

## 5️⃣ Тестування

```bash
# Перевірити статус
curl https://instagram-picker.onrender.com/health

# Спробувати парсинг
curl -X POST https://instagram-picker.onrender.com/api/instagram/comments \
  -H "Content-Type: application/json" \
  -d '{"postUrl": "https://www.instagram.com/p/XXXXX/"}'
```

---

## 🎯 Альтернативи (якщо Render не підходить)

### Railway.app
```bash
# Встановити CLI
npm install -g @railway/cli

# Логін
railway login

# Деплой
railway up
```

### Vercel (тільки для serverless)
```bash
# Встановити CLI
npm install -g vercel

# Деплой
vercel
```

---

## ⚠️ Важливо для безкоштовного хостингу

### Render.com Free Tier обмеження:
- ✅ 512 MB RAM
- ✅ Засинає після 15 хв неактивності
- ✅ Перший запит після сну: ~30 сек
- ✅ 750 годин/місяць безкоштовно

### Рекомендації:
1. **Puppeteer буде працювати**, але повільно
2. **Перший запит довгий** - попередьте користувачів
3. **Для production** краще платний план ($7/міс)

---

## 📊 Структура файлів для деплою

```
www.wask.co/
├── server.js              # ✅ Основний сервер
├── package.json           # ✅ Залежності
├── Procfile              # ✅ Для Render/Heroku
├── render.yaml           # ✅ Конфігурація Render
├── .gitignore            # ✅ Ігнорувати node_modules
└── picker-local.html     # Фронтенд (не деплоїться на Render)
```

---

## 🔧 Після деплою

### Отримати URL бекенду:
```
https://YOUR-APP-NAME.onrender.com
```

### Оновити фронтенд:
```javascript
const API_URL = 'https://YOUR-APP-NAME.onrender.com';
let useRealAPI = true;
```

### Відкрити фронтенд локально:
```
http://localhost:8080/www.wask.co/picker-local.html
```

Або захостити фронтенд на:
- **Netlify** (безкоштовно, для статичних файлів)
- **Vercel** (безкоштовно)
- **GitHub Pages** (безкоштовно)

---

## ✅ Готово!

Тепер у вас:
- ✅ Бекенд на Render.com (безкоштовно)
- ✅ Puppeteer працює в хмарі
- ✅ Справжні Instagram коментарі
- ✅ Випадковий вибір переможця

**Хочете швидше?** Я можу допомогти з деплоєм! 🚀

