# ⚡ Швидкий старт: Підключення справжніх коментарів Instagram

## 🎯 3 способи отримати реальні коментарі

---

## 1️⃣ Ручний імпорт (РЕКОМЕНДУЄТЬСЯ для початку)

### ✅ Переваги:
- Працює відразу, без налаштувань
- Працює з будь-якими постами
- Не потребує API ключів
- Повний контроль над даними

### 📝 Як використовувати:

1. **Відкрийте інтерфейс:**
```
http://localhost:8080/www.wask.co/picker-local.html
```

2. **Натисніть кнопку:**
```
📋 Імпортувати коментарі вручну
```

3. **Скопіюйте коментарі з Instagram:**

**Спосіб A: Вручну**
- Відкрийте пост в Instagram
- Скопіюйте коментарі у форматі:
```
@username1: Текст коментаря
@username2: Ще один коментар
@username3: І ще один @друг
```

**Спосіб B: Через консоль браузера (швидше)**
1. Відкрийте пост в Instagram
2. Натисніть F12 (Developer Tools)
3. Вкладка Console
4. Вставте цей код:

```javascript
// Скопіювати всі коментарі
copy(Array.from(document.querySelectorAll('ul ul li')).map(el => {
  const user = el.querySelector('a')?.innerText || '';
  const text = el.querySelector('span')?.innerText || '';
  return user && text ? `@${user}: ${text}` : null;
}).filter(Boolean).join('\n'));

// Результат автоматично скопійовано в буфер обміну!
```

5. Вставте в поле імпорту (Ctrl+V)
6. Натисніть "✅ Імпортувати"

✨ **Готово! Тепер можна вибирати переможця**

---

## 2️⃣ Власний бекенд (для автоматизації)

### ✅ Переваги:
- Автоматичне завантаження
- Кешування результатів
- Повний контроль

### 🚀 Швидке встановлення:

```bash
# 1. Перейти в папку
cd "www.wask.co"

# 2. Встановити залежності
npm install

# 3. Запустити бекенд
npm start
```

**Бекенд запуститься на http://localhost:3000**

### ⚙️ Активувати в інтерфейсі:

Відкрийте `picker-local.html` в редакторі та знайдіть рядок ~627:

```javascript
let useRealAPI = false; // ← Змініть на true
```

Збережіть файл та перезавантажте сторінку.

✨ **Тепер кнопка "СКАНУВАТИ" буде використовувати бекенд!**

---

## 3️⃣ RapidAPI (найпростіше для production)

### ✅ Переваги:
- Готове рішення
- Надійно працює
- Не потребує сервер

### 💰 Ціна: від $10/міс

### 🔧 Налаштування:

1. **Зареєструйтесь на RapidAPI:**
```
https://rapidapi.com/search/instagram
```

2. **Виберіть Instagram Scraper API**

3. **Скопіюйте ваш API ключ**

4. **Відкрийте `instagram-api.js` та вставте ключ:**

```javascript
const CONFIG = {
  RAPIDAPI_KEY: 'ваш_ключ_тут', // ← Вставте сюди
  ...
};
```

5. **У `picker-local.html` увімкніть API та вкажіть метод:**

```javascript
let useRealAPI = true;

// У функції scanBtn змініть метод:
allComments = await window.InstagramAPI.getComments(url, 'rapidapi');
```

✨ **Готово! Тепер працює через RapidAPI**

---

## 📊 Порівняння методів

| Метод | Складність | Ціна | Швидкість | Надійність |
|-------|-----------|------|-----------|------------|
| 🟢 Ручний імпорт | ⭐ Легко | Безкоштовно | Повільно | 100% |
| 🟡 Власний бекенд | ⭐⭐ Середньо | Безкоштовно | Швидко | 70% |
| 🟢 RapidAPI | ⭐ Легко | $10-50/міс | Швидко | 95% |

---

## 🧪 Тестування

### Перевірити що все працює:

**1. Ручний імпорт:**
```
✅ Відкрити picker-local.html
✅ Натиснути "Імпортувати коментарі вручну"
✅ Вставити тестові дані
✅ Побачити список коментарів
```

**2. Бекенд:**
```bash
# Перевірити здоров'я
curl http://localhost:3000/health

# Отримати демо-коментарі
curl -X POST http://localhost:3000/api/instagram/comments \
  -H "Content-Type: application/json" \
  -d '{"postUrl": "test", "method": "demo"}'
```

**3. Інтерфейс з бекендом:**
```
✅ useRealAPI = true
✅ Бекенд запущений
✅ Ввести URL поста
✅ Натиснути "СКАНУВАТИ"
✅ Побачити коментарі з бекенду
```

---

## 🐛 Що робити якщо не працює?

### Проблема: Модальне вікно не відкривається

**Рішення:**
- Перезавантажте сторінку (Ctrl+F5)
- Перевірте консоль браузера (F12)

### Проблема: "instagram-api.js не завантажується"

**Рішення:**
```bash
# Перевірте що файл існує
ls www.wask.co/instagram-api.js

# Відкрийте в браузері
http://localhost:8080/www.wask.co/instagram-api.js
```

### Проблема: Бекенд не запускається

**Рішення:**
```bash
# Перевірте Node.js
node --version  # Має бути v16+

# Перевстановити залежності
rm -rf node_modules package-lock.json
npm install

# Запустити знову
npm start
```

### Проблема: CORS помилка

**Рішення:**
- Переконайтесь що бекенд має `app.use(cors())`
- Використовуйте локальний сервер, не `file://`

---

## 📚 Детальна документація

- **[API_INTEGRATION_GUIDE.md](./API_INTEGRATION_GUIDE.md)** - Всі методи інтеграції
- **[README_API.md](./README_API.md)** - Повна інструкція
- **[backend-example.js](./backend-example.js)** - Код бекенду
- **[instagram-api.js](./instagram-api.js)** - API модуль

---

## 🎉 Готово!

Тепер у вас є повноцінний інструмент для розіграшів з можливістю:
- ✅ Завантажувати справжні коментарі
- ✅ Фільтрувати дублікати
- ✅ Шукати теги друзів
- ✅ Вибирати випадкового переможця
- ✅ Красива анімація з конфетті

**Успішних розіграшів! 🎊**

---

## 💡 Підказки

**Для малих розіграшів (< 50 коментарів):**
→ Використовуйте ручний імпорт

**Для регулярних розіграшів:**
→ Налаштуйте власний бекенд

**Для бізнесу:**
→ Підключіть RapidAPI або Instagram Graph API

**Для тестування:**
→ Використовуйте демо-режим (за замовчуванням)

