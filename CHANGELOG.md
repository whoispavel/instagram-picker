# 🔧 Виправлення помилки "socket hang up"

## ✅ Що виправлено (28.10.2025 13:20)

### Проблема:
- ❌ Помилка "socket hang up" при спробі сканувати Instagram пост
- ❌ З'єднання розривалося через таймаут

### Виправлення:

#### 1. **Збільшено таймаути:**
- `waitUntil: 'domcontentloaded'` замість `networkidle2` (швидше)
- Таймаут навігації: **45s → 60s**
- Очікування контенту: **3s → 5s**

#### 2. **Краща обробка помилок:**
- Продовжуємо роботу навіть якщо повне завантаження не відбулося
- Детальніші повідомлення про помилки
- Розпізнавання типу помилки (timeout, network, etc.)

#### 3. **Режим відладки:**
- `headless: false` - тепер видно що відбувається в браузері
- Додано більше логів у консоль
- Показуються усі етапи парсингу

---

## 🚀 Як спробувати зараз:

### 1. **Перезавантажте сторінку:**
```
http://localhost:8080/www.wask.co/picker-local.html
```
(Ctrl+F5 для повного перезавантаження)

### 2. **Вставте посилання:**
Наприклад:
```
https://www.instagram.com/reel/DQJXqZ-jGnU/
```

### 3. **Натисніть "СКАНУВАТИ"**

Тепер ви побачите:
- 🌐 Відкриється видиме вікно браузера Chrome
- 📄 Завантажиться Instagram пост
- 📜 Прокрутяться коментарі
- ✅ Результат у фронтенді

**Очікуваний час:** 30-60 секунд (залежить від кількості коментарів)

---

## 🐛 Якщо все одно не працює:

### Варіант 1: Спробуйте інший пост
Деякі пости можуть бути приватними або заблокованими

### Варіант 2: Використайте ручний імпорт
1. Натисніть "📋 Імпортувати коментарі вручну"
2. Вставте коментарі у форматі:
```
@username1: Текст коментаря
@username2: Ще коментар
```

### Варіант 3: Подивіться логи бекенду
В терміналі де запущено `npm start` будуть детальні логи

---

## 📊 Технічні деталі:

### Змінені файли:
- `server.js` - оновлено функцію `scrapeInstagramComments()`

### Нові налаштування:
```javascript
// Було:
headless: 'new'
waitUntil: 'networkidle2'
timeout: 45000

// Стало:
headless: false  // Видимий браузер
waitUntil: 'domcontentloaded'  // Швидше
timeout: 60000  // Більше часу
```

---

## ⚠️ Важливо:

1. **Видимий браузер** - зараз при кожному сканувані буде відкриватися вікно Chrome. Це допомагає бачити що відбувається.

2. **Якщо хочете приховати браузер** - відкрийте `server.js` (рядок 30):
```javascript
headless: false, // Змініть на 'new'
```

3. **Instagram може блокувати** - якщо бачите капчу або блокування, використайте ручний імпорт

---

## ✅ Статус:

- ✅ Бекенд перезапущено
- ✅ Виправлення застосовано
- ✅ Готово до тестування

**Спробуйте зараз!** 🚀

